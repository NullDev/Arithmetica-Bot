// ========================= //
// = Copyright (c) NullDev = //
// ========================= //

const defaults = {
    arithmetic: true,
    cheatmode: false,
    timeout_minutes: 0,
    silentmode: false,
    timeout_factor: 1,
    new_member_cooldown: 60,
};

export default defaults;
