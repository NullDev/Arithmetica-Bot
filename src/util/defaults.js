// ========================= //
// = Copyright (c) NullDev = //
// ========================= //

const defaults = {
    arithmetic: true,
    cheatmode: false,
    mathonly: false,
    pin_highscore: false,
    timeout_minutes: 0,
    timeout_factor: 1,
    new_member_cooldown: 60,
};

export default defaults;
